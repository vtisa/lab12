import { OrdenarPorPrecioPipe } from './ordenar-por-precio.pipe';

describe('OrdenarPorPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
